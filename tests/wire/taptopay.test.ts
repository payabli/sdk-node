// This file was auto-generated by Fern from our API Definition.

import * as Payabli from "../../src/api/index";
import { PayabliClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("TaptopayClient", () => {
    test("ActivationChallenge (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "8cfec329267", deviceId: "499585-389fj484-3jcj8hj3" };
        const rawResponseBody = {
            isSuccess: true,
            responseText: "Success",
            responseData: { code: "748801", expiresAt: "2026-09-10T20:28:27.5860203Z", alreadyIssued: false },
        };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.taptopay.activationChallenge({
            entry: "8cfec329267",
            deviceId: "499585-389fj484-3jcj8hj3",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("ActivationChallenge (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "entry", deviceId: "deviceId" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.taptopay.activationChallenge({
                entry: "entry",
                deviceId: "deviceId",
            });
        }).rejects.toThrow(Payabli.BadRequestError);
    });

    test("ActivationChallenge (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "entry", deviceId: "deviceId" };
        const rawResponseBody = { isSuccess: true, responseText: "responseText" };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.taptopay.activationChallenge({
                entry: "entry",
                deviceId: "deviceId",
            });
        }).rejects.toThrow(Payabli.UnauthorizedError);
    });

    test("ActivationChallenge (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "entry", deviceId: "deviceId" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.taptopay.activationChallenge({
                entry: "entry",
                deviceId: "deviceId",
            });
        }).rejects.toThrow(Payabli.ForbiddenError);
    });

    test("ActivationChallenge (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "entry", deviceId: "deviceId" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.taptopay.activationChallenge({
                entry: "entry",
                deviceId: "deviceId",
            });
        }).rejects.toThrow(Payabli.NotFoundError);
    });

    test("ActivationChallenge (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });
        const rawRequestBody = { entry: "entry", deviceId: "deviceId" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v2/device/taptopay/activate/challenge")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.taptopay.activationChallenge({
                entry: "entry",
                deviceId: "deviceId",
            });
        }).rejects.toThrow(Payabli.InternalServerError);
    });
});
