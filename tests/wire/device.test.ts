// This file was auto-generated by Fern from our API Definition.

import * as Payabli from "../../src/api/index";
import { PayabliClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("DeviceClient", () => {
    test("Challenge (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            responseCode: 1,
            pageIdentifier: "",
            roomId: 0,
            isSuccess: true,
            responseText: "Success",
            responseData: { code: "748801", expiresAt: "2026-08-13T19:58:27.5860203Z" },
        };

        server
            .mockEndpoint()
            .post("/Device/challenge/8cfec329267")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.device.challenge("8cfec329267");
        expect(response).toEqual(rawResponseBody);
    });

    test("Challenge (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });

        const rawResponseBody = { isSuccess: true, responseText: "responseText" };

        server
            .mockEndpoint()
            .post("/Device/challenge/entry")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.device.challenge("entry");
        }).rejects.toThrow(Payabli.UnauthorizedError);
    });

    test("Challenge (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new PayabliClient({
            maxRetries: 0,
            bearerAuth: { clientId: "YOUR_CLIENT_ID", clientSecret: "YOUR_CLIENT_SECRET" },
            apiKeyAuth: { apiKey: "test" },
            environment: server.baseUrl,
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/Device/challenge/entry")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.device.challenge("entry");
        }).rejects.toThrow(Payabli.InternalServerError);
    });
});
